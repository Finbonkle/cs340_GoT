<!DOCTYPE html>
<html>

  <head>
    <title>Seasons Page</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  
  <body>
    
    <div class="main-story container">
      <div id="pageTitle" align="middle">
	<h1>This is the seasons page.</h1>
      </div>


<h3>GoT Seasons</h3>
<table border='1'>
  <tr>
    <td></td>
    {% for n in snum %}
    <th>{{ n[0] }}</th> 
    {% endfor %}
  </tr>
  {% for c in char %}
  <tr>
    <th>{{ c[1] }}</th>
    {% for n in snum %}
    <td>
      {% for s in seas %}
      {% if s[0] == n[0] %}
      {% if s[1] == c[0] %}
      X
      {% endif %}
      {% endif %}
      {% endfor %}
    </td>
    {% endfor %}
  </tr>
  {% endfor %}
</table>

<h4>Add a character to a Season</h4>
<form id="charSeas" action="/seasons" method="post">
  Add Character: <select name="addChar">
    {% for x in char %}
    <option value="{{ x[0] }}">{{ x[1] }}</option>
    {% endfor %}
  </select>
  <br>
  to Season: <select name="addSeas">
    {% for y in snum %}
    <option value="{{ y[0] }}">{{ y[0] }}</option>
    {% endfor %}
  </select>
  <input type="submit" value="submit">
</form>

<h4>Add an addtional season here:</h4>
<form id="newSeas" action="/season_create" method="post">
  <input type="submit" value="AdditionalSeason">
</form>

    
<div class="footer clearfix">
  <div class="container_12">
    <p class="grid_12" align="middle"><a href="index">Home Page</a> | <a href="characters">Characters</a> | <a href="factions">Factions</a> | <a href="seasons">Seasons</a> | <a href="philosophies">Religions and Philosophies</a> | <a href="create">Add a New Character</a> | <a href="edit">Edit a Character</a> | <a href="search">Search for a Character</a></p>
  </div>
</div>

    
  </body>
  
</html>
